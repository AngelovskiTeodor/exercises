#include <stdio.h>

int main()
{
    int a, b, c, d, sum;
    scanf_s("%d", &a);
    scanf_s("%d", &b);
    scanf_s("%d", &c);
    scanf_s("%d", &d);
    sum = a + b + c + d;
    printf("%d",sum);
    getchar();
    return 0;
}
